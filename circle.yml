machine:
  timezone:
    America/Denver

  node:
    version: 8.1.2

  environment:
    DATABASE_URL: postgresql://@127.0.0.1:5432/palettepicker_test

  databse:
    override:
      - knex migrate: latest
